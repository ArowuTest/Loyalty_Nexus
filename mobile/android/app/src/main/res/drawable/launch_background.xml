<?xml version="1.0" encoding="utf-8"?>
<!-- Was @android:color/white while the app renders NexusTheme.dark() — every cold
     start flashed white before the dark UI appeared, and on a low-end device that
     flash lasts up to a second. Now matches the app background exactly. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/nexus_background" />
</layer-list>
