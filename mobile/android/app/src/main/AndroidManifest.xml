<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required in the MAIN manifest: the debug manifest also declares INTERNET,
         which masked its absence here — release builds would ship with no network
         access at all and every API call would fail. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- record + speech_to_text (Studio: Transcribe / dictation) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- Push notifications (firebase_messaging) — required from Android 13 (API 33) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="Loyalty Nexus"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- ── Deep links ────────────────────────────────────────────────
                 Only MAIN/LAUNCHER existed before, so nothing could open the app
                 from outside it. For a telco loyalty product driven by SMS
                 campaigns that is a functional gap, not polish: a link in a
                 campaign SMS had no way to reach a screen.

                 App Links (verified https). autoVerify requires
                 https://loyaltynexus.ng/.well-known/assetlinks.json to be hosted
                 with this app's signing-certificate SHA-256, or Android silently
                 falls back to a chooser dialog. See docs/MOBILE_DEEP_LINKS.md. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"
                      android:host="loyaltynexus.ng"
                      android:pathPrefix="/app"/>
            </intent-filter>

            <!-- Custom scheme. Works with no hosting at all, so it is the
                 reliable fallback while assetlinks.json is not yet deployed. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="loyaltynexus"/>
            </intent-filter>
        </activity>
        <!-- Firebase-SDK-handled notifications (delivered while the app is in the
             background) are built by the SDK, NOT by our Dart code, so they need
             their own defaults. Without these the SDK falls back to the launcher
             icon, which Android renders as a grey blob in the status bar. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/nexus_notification_accent" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
