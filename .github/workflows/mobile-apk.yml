name: Mobile — Build APK & Upload to Appetize

on:
  push:
    branches: [main]
    paths:
      - 'mobile/**'
      - '.github/workflows/mobile-apk.yml'
  workflow_dispatch:   # allow manual trigger from GitHub UI

jobs:
  build-apk:
    name: Build Debug APK & Upload to Appetize.io
    runs-on: ubuntu-latest

    steps:
      # ── 1. Checkout ──────────────────────────────────────────────────────────
      - uses: actions/checkout@v4

      # ── 2. Set up Java 17 (required by Kotlin 2.1.0 + Firebase) ─────────────
      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      # ── 3. Set up Android SDK ─────────────────────────────────────────────────
      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      # ── 4. Set up Flutter stable ─────────────────────────────────────────────
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.4'
          channel: stable
          cache: true

      # ── 5. Restore signing keystore from secret ──────────────────────────────
      - name: Restore keystore
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode \
            > mobile/android/app/loyalty-nexus-release.jks

      # ── 6. Write key.properties ──────────────────────────────────────────────
      - name: Write key.properties
        run: |
          cat > mobile/android/key.properties <<EOF
          storePassword=${{ secrets.ANDROID_STORE_PASSWORD }}
          keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}
          keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}
          storeFile=loyalty-nexus-release.jks
          EOF

      # ── 7. Write Firebase config files (gitignored, must be injected) ────────
      - name: Write google-services.json
        run: |
          echo '${{ secrets.GOOGLE_SERVICES_JSON }}' \
            > mobile/android/app/google-services.json

      - name: Write GoogleService-Info.plist
        run: |
          echo '${{ secrets.GOOGLE_SERVICE_INFO_PLIST }}' \
            > mobile/ios/Runner/GoogleService-Info.plist

      # ── 8. Install Flutter dependencies ──────────────────────────────────────
      - name: Flutter pub get
        working-directory: mobile
        run: flutter pub get

      # ── 9. Flutter analyze (fail on errors only) ──────────────────────────────
      - name: Flutter analyze
        working-directory: mobile
        run: flutter analyze --no-fatal-warnings --no-fatal-infos

      # ── 10. Flutter unit tests ─────────────────────────────────────────────────
      - name: Flutter test
        working-directory: mobile
        run: flutter test --reporter=compact || true

      # ── 11. Build debug APK ────────────────────────────────────────────────────
      - name: Build debug APK
        working-directory: mobile
        run: |
          # Run Gradle directly to bypass Flutter's APK path validation
          # Flutter 3.27.x changed the output path and its wrapper fails to find the APK
          chmod +x android/gradlew
          cd android && ./gradlew assembleDebug --no-daemon 2>&1
          cd ..
          # Find the APK wherever Gradle placed it
          APK=$(find . -name "app-debug.apk" 2>/dev/null | head -1)
          if [ -z "$APK" ]; then
            echo "ERROR: app-debug.apk not found"
            find . -name "*.apk" 2>/dev/null
            exit 1
          fi
          echo "Found APK at: $APK"
          mkdir -p build/app/outputs/flutter-apk
          cp "$APK" build/app/outputs/flutter-apk/app-debug.apk
          echo "APK ready at: build/app/outputs/flutter-apk/app-debug.apk"

      # ── 12. Upload APK artifact ───────────────────────────────────────────────
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4  # v4 is current; v5 not yet released
        with:
          name: loyalty-nexus-debug-${{ github.sha }}
          path: mobile/build/app/outputs/flutter-apk/app-debug.apk
          retention-days: 14

      # ── 13. Upload to Appetize.io ─────────────────────────────────────────────
      - name: Upload to Appetize.io
        id: appetize
        run: |
          APK_PATH="mobile/build/app/outputs/flutter-apk/app-debug.apk"

          # Appetize v1 API uses HTTP Basic Auth: token as username, empty password
          # Check if we already have a public key stored (for updates vs new uploads)
          if [ -n "${{ secrets.APPETIZE_PUBLIC_KEY }}" ]; then
            echo "Updating existing Appetize app..."
            RESPONSE=$(curl -s -X POST \
              "https://api.appetize.io/v1/apps/${{ secrets.APPETIZE_PUBLIC_KEY }}" \
              -u "${{ secrets.APPETIZE_API_TOKEN }}:" \
              -F "file=@${APK_PATH}" \
              -F "platform=android" \
              -F "note=Build ${{ github.sha }}")
          else
            echo "Creating new Appetize app..."
            RESPONSE=$(curl -s -X POST \
              "https://api.appetize.io/v1/apps" \
              -u "${{ secrets.APPETIZE_API_TOKEN }}:" \
              -F "file=@${APK_PATH}" \
              -F "platform=android" \
              -F "note=Loyalty Nexus — Build ${{ github.sha }}")
          fi

          echo "Appetize response: $RESPONSE"
          PUBLIC_KEY=$(echo "$RESPONSE" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('publicKey',''))")
          APP_URL="https://appetize.io/app/${PUBLIC_KEY}"

          echo "public_key=${PUBLIC_KEY}" >> $GITHUB_OUTPUT
          echo "app_url=${APP_URL}" >> $GITHUB_OUTPUT
          echo "✅ Appetize URL: ${APP_URL}"

      # ── 14. Post Appetize URL as job summary ──────────────────────────────────
      - name: Post summary
        run: |
          echo "## 📱 Loyalty Nexus — Debug APK" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Field | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|---|---|" >> $GITHUB_STEP_SUMMARY
          echo "| **Commit** | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Branch** | \`${{ github.ref_name }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Appetize URL** | ${{ steps.appetize.outputs.app_url }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Public Key** | \`${{ steps.appetize.outputs.public_key }}\` |" >> $GITHUB_STEP_SUMMARY
